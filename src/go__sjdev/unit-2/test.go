package main
import "fmt"

func main(){
	var s string
	fmt.Println(s)
	var i ,j, k int
	var b ,f ,s =true, 3.2 ,"dada"
	i ,j := 1, 2
	f, err := os.Open(name)
	if err != nil {
		return err
	}
	f.Close
}